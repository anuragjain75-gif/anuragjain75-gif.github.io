{
  "name": "thelilamaya",
  "version": "1.0.0",
  "private": true,
  "description": "The Lilamaya",
  "scripts": {
    "dev": "hugo server",
    "build": "hugo && npx pagefind --site public"
  },
  "devDependencies": {
    "pagefind": "^1.5.2"
  }
}
